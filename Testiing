Hi Rahil,

I completed the test exactly as suggested.

I tested the same request from:

* macOS using curl
* Windows (office laptop) using Invoke-RestMethod
* Our Python application

In all three cases:

* OAuth token generation succeeds.
* The endpoint is reachable.
* The projectId and X-Project-Id headers are present.
* The request body uses:

```json
{
  "model": "azure/gpt-5.4_2026-03-05",
  "messages": [
    {
      "role": "system",
      "content": "You are a concise assistant."
    },
    {
      "role": "user",
      "content": "Reply with exactly: gateway connectivity test ok"
    }
  ]
}
```

The response is consistently:

```
HTTP 403
Authorization failed. Verify that the deploymentName, modelName are correct.
```

Since the same request fails from curl, PowerShell, and the application, it doesn't appear to be an application or environment issue.

Could you please verify:

1. Is `azure/gpt-5.4_2026-03-05` the exact value that should be used in the `model` field?
2. Is there a separate deployment name that should be used instead?
3. Does project `3fccc5df-159d-47a0-b42d-4d1e49897153` currently have access to this deployment?

Thanks!
